# 二维单预测交错网格拉氏格式

本目录下为对Caramana, et al(1998) 中格式的初步尝试, 仅使用单纯预测(而无矫正), 理论一阶精度, 且暂时不添加人工黏性.

## 数值格式

相比于 1D VNR, Caramana 的格式主要有两点不同, 一是对二维几何的处理, 二是离散中的相容性, 即总能量代数守恒.

### 二维几何

物理量的分布仍继承1D VNR 的交错思想, 但二维中可以更明显地将其理解为对偶网格. 原网格单元$z$上存储内能信息

> 单元质量$M_z$, 密度$\rho_z$, 压强$P_z$, 比内能$e_z$, 

及其几何信息

> 顶点$x_i$, 边$e_i^z := x_{i+1} - x_i$, 外法向量$N_i^z:|N_i|=|e_i|$, 面积$A_z$.

在对偶网格单元$p$上存储动量信息

> 单元质量$M_p$, 中心位置$x_p$, 速度$v_p$.



此外, 原网格与对偶网格相互交错, 使得每个四边形都会被对偶网格剖分为四份, Caramana 称之为角块(corner), 反过来四个角块就可以组合成一个单元或对偶单元, 以此作为两套网格的沟通媒介. 将单元$z$ 的顶点$x_i$ 上的角块记为$X_i^z$, 存储几何信息

> 角向量$C_i^z :=\frac12 (N_{i-1}^z+N_i^z)$,

动量信息

> 角质量$m_i^z = \rho_z^0 A_{\mathrm{corner},i}^0$, 角力$f_i^z=P_z C_i^z$.

角块与两种单元的关系为

质量关系
$$
M_z = \sum_{p\in z} m_p^z,\quad M_p = \sum_{z\ni p}m_p^z,
$$
动量关系
$$
a_p = \frac1{M_p}\sum_{z\ni p}f_p^z.
$$

### 相容性(代数守恒)

前面使用角力来推动对偶单元(单元顶点) 移动, 下面继续使用角力更新内能以确保总能量守恒
$$
e^{n+1/2}_z = e_z^n - \frac{\Delta t}{M_z}\sum_{p\in z}f_p^{z, \sigma} \cdot v_p^{n+1/2},
$$
其中半时刻速度为算术平均.

注意, 这与
$$
e += -P(\nabla\cdot v) \cdot \frac{dt}{\rho}
$$
在数值上不等价. 

不过具体是怎么个事, 我还没搞明白.

#### 关键检查点

由于格式在代数上确保总能量守恒, 所以在程序测试中可以时刻检查
$$
\frac{E_T(t^n)-E_T(t^0)}{E_T(t^0)} \approx 10^{-14},
$$
其中总能量
$$
E_T := \sum_z M_z e_z + \sum_p \frac12 M_p |v_p|^2.
$$

## 程序实现

整套网格用一个结构体还是一个单元一个? GPT 说前者更适合计算, 大概像这样

````C
typedef struct {
    double *area;
    double *pressure;
    double *density;
    double *temperature;
} Grid;

Grid grid;

grid.area        = malloc(N * sizeof(double));
grid.pressure    = malloc(N * sizeof(double));
grid.density     = malloc(N * sizeof(double));
grid.temperature = malloc(N * sizeof(double));
````

调用的时候

````C
grid.area[i]
grid.pressure[i]
grid.density[i]
grid.temperature[i]
````

这样的话想表示从属关系就适合用指标而非指针了.

### 数据结构

使用数组结构体(sturct of array, SoA) 管理三套网格信息.

````C
typedef struct {
    double *area;
    double *pressure;
    double *density;
    double *temperature;
} Grid;

Grid grid;

grid.area        = malloc(N * sizeof(double));
grid.pressure    = malloc(N * sizeof(double));
grid.density     = malloc(N * sizeof(double));
grid.temperature = malloc(N * sizeof(double));
````

调用的时候

````C
grid.area[i]
grid.pressure[i]
grid.density[i]
grid.temperature[i]
````



尽管不会使用节点编号判断从属或相等, 但使用节点编号可以将拓扑信息与几何信息分离, 更新节点位置时仅更新编号-位置映射, 不用动结构内部

Grid 结构存储原网格各单元的信息. 拓扑信息为顶点编号, 所含角块编号. 物理信息为单元质量, 密度, 比内能. 压强信息下放到角块上, 这样可以避免计算角力时返回找父级单元.

````C
typedef struct {
    // topology info
    size_t *x;
    size_t *corners;
    // physical info
    double *mass;
    double *density;
    double *internal;
} Grid;
````

DualGrid 结构存储对偶网格单元的信息. 拓扑信息为中心编号(即对应原单元的顶点), 所含角块编号. 

````C
typedef struct {
    // topology info
    size_t *center;
    size_t *corners;
    // physical info
    double *mass;
    double *velocity;
} DualGrid;
````

Corners 结构存储角块上的信息. 物理信息: 质量, 压强, 力.

````C
typedef struct {
    double *vertices;
    double *mass;
    double *pressure;
    double *force;
} Corners;
````

使用一个数组记录编号-坐标映射

````C
double *coordinates;
````



#### 遍历编号

设X方向有N 个单元, 则

含虚拟单元的编号 iGhostX $\in [0,N+2)$, 物理单元(不含虚拟单元) 的编号 iCellX $\in [0, N)$.

含虚拟对偶单元的编号 iDualGhostX $\in [0,N+1)$, 物理对偶单元的编号 iDualCellX $\in [0, N-1)$.

角块的编号 iCorner $\in [0,2N)$.



### 算法流程

0. 读取计算参数

   时间终点T, CFL 数.

1. 几何初始化

   读取easymesh网格, 填充编号-坐标映射, 构建Grid gird_n, grid_new 结构, 并填充拓扑信息
   根据几何信息构建DualGrid dualgrid_n, dualgrid_half 与 Corners corners, 并填充拓扑信息

2. 物理初始化

   根据初值条件填充各结构体中的物理量初值.

3. 确定时间步长

   根据声速和CFL 数计算时间步长$\tau$.

4. 时间推进

   计算角力 corner.force

   用角块加速度更新对偶单元的速度存入 dualgrid_half.veolicty

   更新单元内能 grid_new.internal, 对偶单元(单元顶点)位置 dualgrid_half.position, 计算单元面积更新单元密度 grid_new.density, 角块压强 corners.pressure.

5. 施加边界条件

6. 时间更新

   $n \leftarrow n+1$, $t \leftarrow t+\tau$. 若$t=T$, 结束; 否则回到3. 







### 渐进测试

#### 准备

使用EasyMesh 生成二维非均匀网格;

使用C 读取和输出网格;

使用Opendx 绘制网格图像.

#### 1.几何测试

任意非均匀凸四边形网格, 应当有
$$
\sum_{i\in z} C_i = 0.
$$
此外, 任意给定节点速度分布, 应当有
$$
\frac{A(x+\epsilon v) - A(x)}{\epsilon} \rightarrow \sum_i C_i\cdot v_i.
$$

#### 2. 均匀压强

初值条件
$$
P=P_0, \quad v=0,
$$
下, 所有内点应当无加速度.